apiVersion: v1
kind: Service
metadata:
  creationTimestamp: null
  labels:
    io.kompose.service: {{ .Values.edge.name }}
  name: {{ .Values.edge.name }}
spec:
  ports:
  - name: {{ .Values.edge.host.name }}
    port: {{ .Values.edge.host.port }}
    targetPort: {{ .Values.edge.host.port }}
  selector:
    io.kompose.service:  {{ .Values.edge.name }}
status:
  loadBalancer: {}
